import { StyleSheet } from "react-native";

export default StyleSheet.create({
  searchbar: {
    alignItems: "center",
    padding: 6,
    marginTop: 8,
    marginRight: 16,
    fontSize: 17,
  }
})
